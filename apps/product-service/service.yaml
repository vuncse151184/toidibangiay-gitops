apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: toidibangiay-prod
spec:
  selector:
    app: product-service
  ports:
    - port: 4001
      targetPort: 4001
  type: ClusterIP
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: toidibangiay-prod
spec:
  selector:
    app: notification-service
  ports:
    - port: 3007
      targetPort: 3007
  type: ClusterIP
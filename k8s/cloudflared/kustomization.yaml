apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - deployment.yaml
# NOTE: the `cloudflared-credentials` Secret is created out-of-band (see
# README.md), not committed — same pattern as gateway-secrets.
